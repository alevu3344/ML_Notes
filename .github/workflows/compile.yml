name: Build LaTeX and deploy on GitHub Releases
on:
  push:
    tags: '*'
    branches-ignore:
      - 'autodelivery**'
      - 'bump-**'
      - 'renovate/**'
    paths-ignore:
      - 'README.md'
      - 'CHANGELOG.md'
      - 'LICENSE'
      - 'renovate.json'
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python and Dependencies
        run: |
          apt-get update
          apt-get install -y python3-venv curl git
          
          # Install uv
          curl -LsSf https://astral.sh/uv/install.sh | sh
          
          # Export dependencies from uv.lock
          $HOME/.local/bin/uv export --format requirements-txt --output-file requirements.txt
          
          # Create virtual environment to avoid system package conflicts
          python3 -m venv .venv
          
          # Install dependencies into the virtual environment
          .venv/bin/pip install -r requirements.txt

      - name: Compile LaTeX document
        run: |
          # latexmk will read .latexmkrc automatically
          latexmk -pdf main.tex

      - name: Upload PDF as artifact
        uses: actions/upload-artifact@v4
        with:
          name: PDF
          path: main.pdf

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: main.pdf